# Netra headless render stills

Generated with `build123d.render` (iso views; section = Plane.YZ).

SVG files embed the PNG stills as base64 data URIs (GitHub MCP cannot round-trip raw PNG bytes via text upload). Matching `*.png.b64` sidecars hold the same payloads for exact decode.
